target-version = "py310"
line-length = 100

[lint]
select = ["E", "F", "I", "UP", "B", "W"]
ignore = ["E501"]

[lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends"]

[lint.per-file-ignores]
"ingestion/main_ingest.py" = ["E402"]
