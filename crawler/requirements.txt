requests==2.32.3
tqdm==4.67.1
python-dotenv==1.0.1
pandas==2.2.3

# Pacote compartilhado (entidades comuns). No Docker é instalado pelo Dockerfile.
# Localmente, instale em modo editável a partir desta pasta:
#   pip install -e ../shared
